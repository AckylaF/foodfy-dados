

    <div class="fields">
        <p>Nome</p>
        <input 
            type="text"
            name="name"
            value="{{chef.name}}"
            placeholder="Use apenas um sobrenome"
        >
    </div>

    <div class="fields">
        <p>Avatar</p>
        <input 
            type="text" 
            name="avatar_url"
            value="{{chef.avatar_url}}"
            placeholder="Sua foto de perfil"
        >
    </div>